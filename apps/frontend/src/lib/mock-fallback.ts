/**
 * MOCK DATA ONLY - replace with real API later
 * Re-exports centralized demo data for backward compatibility.
 */
export {
  responseTimeTrend,
  uptimeTrend,
  incidentTrend,
  monitorDistribution,
  initialRecentAlerts as recentAlerts,
} from '@/lib/mock-data';

export { useDashboardStats } from '@/hooks/use-dashboard';
