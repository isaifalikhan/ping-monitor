/**
 * MOCK DATA ONLY - replace with real API later
 * When true, feature pages use local demo state instead of backend APIs.
 * Authentication always uses the real API.
 */
export const DEMO_MODE = true;
